name: 'retail_elt'
version: '1.0.0'
config-version: 2

profile: 'retail_elt'

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  retail_elt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
