.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# ConcertNOW's SQLite deploy DB is generated automatically by app.py on
# first run (see init_db()) — never commit the file itself.
projects/09-concert-trip-planner/deploy/concertnow.db

# Project 10's DuckDB warehouse + dbt build artifacts are regenerated by
# the pipeline every run (locally or in CI) — never commit them. The
# data/raw_daily/*.csv fixtures ARE committed (small, ~5MB total, and the
# whole point is a self-contained pipeline with no external dependency).
projects/10-retail-elt-pipeline/warehouse.duckdb
projects/10-retail-elt-pipeline/dbt_project/target/
projects/10-retail-elt-pipeline/dbt_project/logs/
projects/10-retail-elt-pipeline/dbt_project/dbt_packages/
projects/10-retail-elt-pipeline/dbt_project/.user.yml

# Raw data dumps over ~20MB are noted with a source link in each project's data/README.md
# instead of being committed, to keep the repo lightweight.
projects/01-retail-customer-analytics/data/online_retail.csv
projects/01-retail-customer-analytics/tableau/transactions.csv
projects/01-retail-customer-analytics/tableau/customers.csv
projects/03-data-job-market-trends/data/gsearch_jobs_2022.csv
projects/07-fake-job-detection/data/fake_job_postings.csv
projects/08-galaxy-sciviz/data/TipsyGalaxy.tar.gz
projects/08-galaxy-sciviz/data/TipsyGalaxy/

# Raw uploaded source material not part of the polished site — the relevant
# PDF/notebooks were reorganized into projects/07-fake-job-detection/ and
# projects/08-galaxy-sciviz/. "Team DataDucks final" also contains a real
# .env with live DB credentials — never commit that folder.
/fake job project/
/Team DataDucks final/
/SciViz.ipynb
/Extra＿Credit＿ Word:Text Data.ipynb
